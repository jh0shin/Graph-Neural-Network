>>>>>> import torch
       
       
>>>>>> def broadcast(src: torch.Tensor, other: torch.Tensor, dim: int):
    8:     if dim < 0:
    4:         dim = other.dim() + dim
    8:     if src.dim() == 1:
    8:         for _ in range(0, dim):
>>>>>>             src = src.unsqueeze(0)
   12:     for _ in range(src.dim(), other.dim()):
    4:         src = src.unsqueeze(-1)
    8:     src = src.expand_as(other)
    8:     return src
